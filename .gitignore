.DS_Store
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.env
.env.prod
# Operator-only prod config & secrets (helm values, trunks, cutover runbook).
# Lives in-repo for convenience but is never committed; see infra/README.md.
infra/private/
node_modules/
.next/
out/
dist/
*.tfstate*
*.tfplan
.terraform/
.terraform.lock.hcl
.coverage
coverage.xml
# Supabase CLI local scratch (supabase/.temp etc.) — not part of the deploy.
supabase/

# Layer 3 audio harness: recordings of real calls and the synthesis cache.
# Both are reproducible and neither belongs in history.
worker/tests/audio/artifacts/
worker/tests/audio/.cache/
